# Legacy SCANNER_* variables are still accepted for compatibility, but new
# setups should use the PROXBOX_* names below.
PROXBOX_MODE=off
PROXBOX_INTERVAL_SECONDS=900
PROXBOX_CRON_EXPRESSION=*/15 * * * *
PROXBOX_RESTART_DELAY_SECONDS=0
TZ=UTC
